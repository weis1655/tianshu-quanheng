# 天枢权衡 — AI 修复规范

## 修复原则
1. **最小改动**：只改 1-2 行，不重构
2. **禁止修改**：配置文件、依赖文件、测试文件
3. **验证要求**：编译通过 + 模块导入通过
4. **单问题上限**：改动不超过 50 行

## 禁止修改的文件
- `config/` 目录
- `.env` 及 `.env.*`
- `requirements.txt` / `pyproject.toml`
- `agents/__init__.py`

## 修复流程
1. 定位问题 → 2. 最小改动 → 3. 编译验证 → 4. 提交 → 5. 等待 PR 审核
