# 天枢权衡 · 全量审计问题清单

> 生成时间：2026-07-17
> 审计范围：agents/ 52文件 + scripts/ 14文件 + main.py + 配置
> 审计类型：复用性问题 + 自动化缺口 + 硬编码一次性脚本

---

## 🔴 高优先级核心改造

| 问题ID | 类型 | 所在模块 | 问题描述 | 当前调用方式 | 改造价值 |
|:------:|:----:|:---------|:---------|:------------:|:--------:|
| R01 | 复用性 | `base_agent.py`/`weekly_review_agent.py`/`回头看.py` | `add_market_prefix` 3份独立重复实现，代码完全相同 | 各模块各自维护 | 消除3份重复，统一维护点 |
| R02 | 复用性 | `pool_updater.py` | 私有 `_safe_read_json`/`_safe_write_json` 与 `safe_file_utils.py` 标准实现重复 | 私有方法 | 统一到 `safe_file_utils` |
| R03 | 复用性 | `decision_agent.py`/`pool_updater.py` | `_fetch_current_prices` 2份重复实现，行情查询逻辑相同 | 各自独立调用 | 消除重复，统一行情查询入口 |
| R05 | 复用性 | 全agents/ scripts/ | 30+处裸`json.loads(path.read_text())`，未使用`safe_file_utils` | 直接读写文件 | 统一安全文件读写，消除异常风险 |
| A01 | 自动化 | 五池管理 | 池JSON文件无自动备份，数据丢失风险 | 无 | 每日收盘后自动备份 |
| A02 | 自动化 | 日志系统 | 日志清理Cron已暂停，logs/可能堆积 | 已暂停 | 恢复清理，设30天 retention |

## 🟡 中优先级效率提升

| 问题ID | 类型 | 所在模块 | 问题描述 | 当前调用方式 | 改造价值 |
|:------:|:----:|:---------|:---------|:------------:|:--------:|
| R04 | 复用性 | `conditional_order.py`/`quote_provider.py` | `fetch_quote`/`fetch_batch` 2份独立实现，接口不同 | 各自独立 | 统一行情查询接口 |
| R06 | 复用性 | 15+ agents/ files | `sys.path.insert(0, ...)` 重复，本应由 `path_config.py` 管理 | 各文件顶部 | 统一路径配置 |
| R08 | 复用性 | `quote_provider.py` | `safe_float`/`safe_int`/`safe_str` 仅1处标准实现，但多处内联 `float(v)` | 未推广使用 | 推广安全类型转换 |
| A03 | 自动化 | `backtest_sandbox.py` | 批量回测全手动，无自动调度 | 手动python调用 | 注册为每周自动回测 |
| A04 | 自动化 | `auto_tasks.py` | 聚合入口Cron已建但未执行过 | Cron存在但未验证 | 验证修复自动执行 |
| A05 | 自动化 | `tianshu_memory.py` | MemPalace记忆无定期压缩优化 | 仅运行时触发 | 每周自动压缩 |

## 🟢 低优先级规范优化

| 问题ID | 类型 | 所在模块 | 问题描述 | 当前调用方式 | 改造价值 |
|:------:|:----:|:---------|:---------|:------------:|:--------:|
| R09 | 复用性 | `scripts/win_rate_analyzer.py` | 废弃存根，功能已迁移至回头看.py | 存根返回空 | 删除或标记废弃 |
| A06 | 自动化 | `tianshu_email_template.py` | 无自动调度，需手动触发 | 手动调用 | 考虑整合到各阶段Cron |

---

## 优先级分布

| 优先级 | 数量 | 占比 |
|:------:|:----:|:----:|
| 🔴 高优先级 | 6 | 43% |
| 🟡 中优先级 | 6 | 43% |
| 🟢 低优先级 | 2 | 14% |
| **合计** | **14** | **100%** |