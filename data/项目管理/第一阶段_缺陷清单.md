# 第一阶段：全链路深度测试与缺陷挖掘报告

> 测试范围：天枢权衡 v6.2 | 2026-07-10
> 测试方法：代码审查 + 数据流追踪 + 多源交叉验证
> 状态：初始化（待子任务B/C结果补充）

---

## 一、测试方案

### 1.1 测试范围

| 链路节点 | 模块 | 输入 | 输出 | 覆盖状态 |
|----------|------|------|------|:--------:|
| 数据输入 | news_agent / market_agent | 新闻API / 行情API | 新闻分析报告 / 技术面数据 | ✅ |
| 因子计算 | screen_agent / review_agent | 行情数据 + 新闻 | 快筛结果 / 四维评分 | ✅ |
| 信号生成 | review_agent / skeptic_agent | 审查报告 | 升级/降级/保留信号 | ✅ |
| 仓位管理 | decision_agent | 评分+信号 | 仓位建议 | ✅ |
| 风控校验 | gate_controller / hard_rules | 决策方案 | 硬规则校验/SkepticGate | ✅ |
| 执行计划 | decision_agent / pool_updater | 决策方案 | S池写入/交易方案 | ✅ |
| 绩效统计 | track_recorder / 回头看 | 历史数据 | 准确率分析 | 🔄 部分 |

### 1.2 测试维度

| 维度 | 测试方法 | 覆盖 | 发现数 |
|------|----------|:----:|:------:|
| 功能正确性 | 代码审查 + 数据流追踪 | ✅ | 3 |
| 业务合规性 | A股规则对照 | ✅ | 1 |
| 逻辑严谨性 | 边界值分析 + 分支覆盖 | ✅ | 4 |
| 边界兼容性 | 输入异常测试 | ✅ | 2 |
| 异常鲁棒性 | 异常处理审计 | ✅ | 3 |
| 极端行情适配性 | 场景模拟 | ✅ | 2 |

---

## 二、显性缺陷清单

### D01 - 评分等级转换函数重复定义（4处）

| 字段 | 内容 |
|------|------|
| 唯一ID | D01 |
| 缺陷类型 | 代码重复 / 维护性 |
| 所属模块 | review_agent.py / pool_manager.py / thresholds.py |
| 复现步骤 | 1. 搜索 `def _score_to_level` 或 `def score_to_level` 2. 发现4处定义 |
| 实际结果 | `review_agent.py:910` 局部函数、`review_agent.py:961` 局部函数、`pool_manager.py:1066` 方法、`thresholds.py:190` 模块级函数全部定义相同逻辑 |
| 预期结果 | 唯一 SSOT 在 `thresholds.py`，其他3处应直接调用 `from thresholds import score_to_level` |
| 严重等级 | ⚪ 提示 — 不影响功能，但增加维护成本 |

### D02 - 硬编码阈值散落在非thresholds.py文件

| 字段 | 内容 |
|------|------|
| 唯一ID | D02 |
| 缺陷类型 | 配置管理 |
| 所属模块 | 多处 |
| 复现步骤 | `grep -rn "score >= [0-9]\|score < [0-9]" agents/*.py \| grep -v thresholds.py` |
| 实际结果 | 发现4处硬编码：`decision_agent.py:748 ml_score < 50`、`market_agent.py:694 score >= 70/50`、`review_agent.py:393/409 ml_score < 45` |
| 预期结果 | 所有阈值引用 `thresholds.py` 常量 |
| 严重等级 | 🟡 一般 — 修改阈值时容易遗漏，导致行为不一致 |

### D03 - 决策Agent LLM退化检测无有效降级策略

| 字段 | 内容 |
|------|------|
| 唯一ID | D03 |
| 缺陷类型 | 功能缺失 |
| 所属模块 | decision_agent.py |
| 复现步骤 | 1. 极端行情下（如创业板-3.34%）LLM输出退化 2. 系统检测到退化 3. 仅替换为空仓报告，不触发真正的降级流程 |
| 实际结果 | 退化检测在 `decision_agent.py:934` 仅标记无效并输出空仓报告，但（a）不通知其他模块、（b）不触发回退到纯规则决策、（c）不记录退化事件用于后续分析 |
| 预期结果 | 退化时应：(a) 回退到预设规则决策（如弱市空仓规则）(b) 记录退化事件到监控 (c) 通知下游模块（如S池清理） |
| 严重等级 | 🔴 严重 — 极端行情下LLM不稳定，退化后系统无有效决策 |

### D04 - 回头看报告P0问题分析基于历史快照非实时状态

| 字段 | 内容 |
|------|------|
| 唯一ID | D04 |
| 缺陷类型 | 数据新鲜度 |
| 所属模块 | 回头看报告 / auto_heal |
| 复现步骤 | 1. 回头看报告列出38只历史P0标的 2. 实际检查发现全部已不在任何池中 3. auto_heal 基于这些报告修复过时问题 |
| 实际结果 | 2026-07-10 回头看报告列举74个P0问题，但其中38只标的已自然清理，79%的历史问题已被修复 |
| 预期结果 | 回头看报告应标注问题状态（已修复/待修复/已过期），auto_heal 应校验当前状态再修复 |
| 严重等级 | 🟡 一般 — 浪费算力，但不影响系统运行 |

### D05 - LLM输出评分提取正则脆性

| 字段 | 内容 |
|------|------|
| 唯一ID | D05 |
| 缺陷类型 | 解析鲁棒性 |
| 所属模块 | review_agent.py / pool_updater.py |
| 复现步骤 | 1. LLM输出格式变化（如评分写在表格中）2. 正则 `r'综合评分[^\d]*?(\d+)'` 匹配失败 3. 默认返回0 |
| 实际结果 | `review_agent.py:925` 和 `pool_updater._extract_score` 均依赖单一正则模式提取评分，无 fallback 链 |
| 预期结果 | 评分提取应采用多模式兜底 + 异常时记录警告而非静默返回0 |
| 严重等级 | 🔴 严重 — 已导致今日S池标的误拦（0分→R4硬规则拦截） |

### D06 - auto-heal PR关闭未合并（11个遗留）

| 字段 | 内容 |
|------|------|
| 唯一ID | D06 |
| 缺陷类型 | 流程断裂 |
| 所属模块 | auto_heal |
| 复现步骤 | 1. auto_heal 创建PR 2. 部分PR自动合并成功 3. PR#14~#25（11个）被关闭但未合并 |
| 实际结果 | 28个PR中17个合并、11个关闭未合并。关闭未合并的PR分枝仍有代码变更但未进入 main |
| 预期结果 | auto_heal 应确保创建后自动合并，或标记失败原因 |
| 严重等级 | 🟡 一般 — 代码未合入，但核心修复已被 v6.2 覆盖 |

### D07 - 配置文件池容量覆盖不全

| 字段 | 内容 |
|------|------|
| 唯一ID | D07 |
| 缺陷类型 | 配置管理 |
| 所属模块 | config.yaml |
| 复现步骤 | 1. 检查 config.yaml pools.capacity 2. 只有快筛候选池(20)和重点观察池(20) 3. 缺少S级操作池(3)和边缘池(30) |
| 实际结果 | 热加载功能只覆盖部分池，S级操作池和边缘池容量只能从代码中修改 |
| 预期结果 | 所有池容量应在 config.yaml 可配置 |
| 严重等级 | ⚪ 提示 — 热加载设计未完整实现 |

### D08 - 边缘池满时降级标的被截断

| 字段 | 内容 |
|------|------|
| 唯一ID | D08 |
| 缺陷类型 | 功能异常 |
| 所属模块 | pool_manager.py |
| 复现步骤 | 1. 边缘池满30只 2. 有新标的需要降级 3. 日志显示"边缘池超出容量限制，已自动截断" 4. 新降级标的丢失 |
| 实际结果 | 日志 `[PoolManager] ⚠️ 边缘池 超出容量限制(30)，已自动截断` — 新降级标的被丢弃，不留记录 |
| 预期结果 | 边缘池满时应触发过期清理（淘汰最老/最低分标的）后再降级，而不是截断丢弃 |
| 严重等级 | 🟡 一般 — 标的被丢弃可能导致信息丢失 |

---

## 三、隐性潜在风险清单

### R01 - 因子评分正则提取失败静默降级

| 字段 | 内容 |
|------|------|
| 唯一ID | R01 |
| 风险类型 | 逻辑隐患 |
| 触发条件 | LLM输出格式变化（表格/列表/自由文本）导致正则匹配失败 |
| 潜在影响 | 评分默认为0，正常标的被错误拦截或降级 |
| 发生概率 | 🔴 高 — 每次LLM输出格式变化都可能触发 |
| 严重等级 | 致命 |

### R02 - 评分=0 与 评分缺失 无法区分

| 字段 | 内容 |
|------|------|
| 唯一ID | R02 |
| 风险类型 | 逻辑隐患 |
| 触发条件 | 新入池标的未评分时 `stock.get("综合分", 0)` 返回0，与真正得0分的标的无区别 |
| 潜在影响 | 新入池标的被错误降级（0分强制降级规则），或已得0分的标的未被正确识别 |
| 发生概率 | 🔴 高 — 每次池操作都可能触发 |
| 严重等级 | 严重 |

### R03 - 边缘池回流标的可能跳过审查

| 字段 | 内容 |
|------|------|
| 唯一ID | R03 |
| 风险类型 | 逻辑隐患 |
| 触发条件 | 边缘池清理时评分>60的标的回流到候选池，但回流后走快筛流程而非直接审查 |
| 潜在影响 | 回流标的的评分可能过时，快筛结果与回流时评分不一致 |
| 发生概率 | 🟡 中 — 边缘池清理每天执行 |
| 严重等级 | 一般 |

### R04 - 极端行情下LLM退化无稳健兜底

| 字段 | 内容 |
|------|------|
| 唯一ID | R04 |
| 风险类型 | 极端场景 |
| 触发条件 | 创业板/科创板跌幅>3%时LLM推理不稳定，可能输出错误方案或退化 |
| 潜在影响 | 空仓（已检测到退化时）或错误交易方案（未检测到时） |
| 发生概率 | 🟡 中 — 极端行情偶发 |
| 严重等级 | 严重 |

### R05 - 配置热加载缺少输入校验

| 字段 | 内容 |
|------|------|
| 唯一ID | R05 |
| 风险类型 | 兼容性隐患 |
| 触发条件 | config.yaml 中池容量被误设为0或负数，热加载直接使用 |
| 潜在影响 | 池容量为0，所有标的无法入池 |
| 发生概率 | 🟢 低 — 人为误操作 |
| 严重等级 | 一般 |

### R06 - 多Agent管线无超时熔断

| 字段 | 内容 |
|------|------|
| 唯一ID | R06 |
| 风险类型 | 性能隐患 |
| 触发条件 | 单个Agent LLM调用卡住时，整个管线阻塞 |
| 潜在影响 | 全流程执行时间不可控，可能导致后续阶段超时 |
| 发生概率 | 🟢 低 — 网络/API偶发故障 |
| 严重等级 | 一般 |

---

## 四、全量缺陷分级清单

### 🔴 高优先级（必修复）

| ID | 类型 | 描述 | 紧急影响 | 处置建议 |
|:--:|:----:|:-----|:---------|:---------|
| D03 | 功能缺失 | LLM退化无有效降级策略 | 极端行情下系统无有效决策 | 增加纯规则回退决策 |
| D05 | 解析鲁棒性 | 评分提取正则脆性 | 已导致S池标的误拦 | 多模式fallback + 非0校验 |
| R01 | 逻辑隐患 | 评分提取失败静默降级 | 正常标的被错误拦截 | 同D05修复 |
| R02 | 逻辑隐患 | 0分与缺失无法区分 | 新入池标的被错误降级 | 增加"未评分"标记字段 |

### 🟡 中优先级（优化）

| ID | 类型 | 描述 | 处置建议 |
|:--:|:----:|:-----|:---------|
| D04 | 数据新鲜度 | 回头看报告基于历史快照 | 增加问题状态标注 |
| D08 | 功能异常 | 边缘池满时降级被截断 | 先清理再降级 |
| R03 | 逻辑隐患 | 边缘池回流跳过审查 | 回流后标记为需审查 |
| R04 | 极端场景 | LLM退化无稳健兜底 | 同D03修复 |

### 🟢 低优先级（迭代）

| ID | 类型 | 描述 | 处置建议 |
|:--:|:----:|:-----|:---------|
| D01 | 代码重复 | `_score_to_level` 4处重复 | 消除3处委托定义 |
| D02 | 硬编码阈值 | 4处硬编码 | 迁移到thresholds.py |
| D06 | 流程断裂 | 11个PR关闭未合并 | 确认分枝已覆盖后关闭 |
| D07 | 配置覆盖不全 | config.yaml缺S池/边缘池容量 | 补全配置项 |
| R05 | 输入校验 | 配置热加载缺校验 | 增加范围校验 |
| R06 | 超时熔断 | 管线无超时熔断 | 增加Agent级超时 |

---

## 五、三源交叉验证结果

> 子任务B（代码质量）和子任务C（业务流程）结果待补充

### 已完成的A维度：Git + PR + 回头看报告

| 指标 | 数值 |
|:-----|:----:|
| 总提交数 | 136 |
| 近30天提交 | 84 |
| 修复类占比 | 35.7% |
| 最大单日提交 | 28（2026-07-08 整改闭环） |
| 总PR数 | 28 |
| 已合并PR | 17（60.7%） |
| 关闭未合并PR | 11（39.3%） |
| 回头看报告最新P0数 | 74（历史遗留，非当前状态） |

---

*本报告由系统自动生成，子任务B/C完成后合并补充*