# 修复工单：S池准入链路脱节（续修 ④⑤⑥）

- **关联事件**：2026-09-10 S池3只标的综合分=0（兴发集团/兴森科技/川发龙蟒）
- **主因定性**（盟主）：兴发集团、兴森科技已被 Skeptic 标记量能 high 风险，但
  PoolUpdater 仍将其从重点观察池晋级 S 级操作池，与 Skeptic 裁决存在流程脱节
- **止血完成**：2026-09-10 ①②③ 已上线（见下文「已修复」），工单覆盖剩余 ④⑤⑥
- **根因链路**：`SkepticAgent → 裁决JSON → GateController.read_verdict → decision_agent
  L558 Gate(拦 pools/scored_stocks)` → **断点**：`PoolUpdater.update_s_pool` 对
  blocked_codes 零可见性，宽松兜底经自由文本正则绕过 Gate 语义边界

---

## 已修复（2026-09-10 止血批次）

| 编号 | 措施 | 位置 |
|---|---|---|
| ① | S池写入接入 Skeptic 裁决（硬前置 + 逐标的拦截） | `agents/pool_updater.py` |
| ② | 宽松兜底 action_kw 收窄为带数值行动字段 + 否决语境守卫 | `agents/pool_updater.py` |
| ③ | LLM 自洽交叉校验（声明0只 → S池写入应为0，硬拦截） | `agents/pool_updater.py` |

验证：`verify_s_pool_fix.py` 5/5 场景通过（含反向不误杀 + 容量守卫）；
`tests/test_s_pool.py` 12/12；`tests/test_pool_manager.py` 全通过。

---

## 待修（本工单范围）

### ④ 评分/逻辑提取增强（次要，低优先）
`pool_updater._extract_score` 正则不容忍虚词，原文"评分**仅**42分"→ 无匹配 → 0。
- 修复：正则加 `(?:仅|约|为|达|共)?\s*[：:]?` 容忍虚词
- 仍提取不到时回退审查汇总表该标的评分
- 再无则标 `None`（区分「无评分」与「0分」的语义）
- 附带：`_extract_logic_snippet` 只找"核心驱动/逻辑支撑"字样，07-21 后格式已无此字段，
  导致 S池核心逻辑全空。需兼容新格式的驱动描述行。

### ⑤ 数据修正（已随止血自然消解，仅确认）
2026-09-10 当日那3只标的本就无效。修复后次日 `_check_s_pool_overlap` + T+1 过期清理会
自动回收；若需立即清空可手动置空 `五池管理/S级操作池.json` 的 `stocks` 字段。
（当前未手动清空——保留作为脱节事件的历史证据，供复盘引用。）

### ⑥ S池准入契约定义 + 准入测试（架构级，盟主点名复盘项）
- **契约**：「重点观察池 → S级操作池」晋级路径此前从无 Skeptic 状态校验环节。
  须明确定义：
  1. Skeptic 通过（`has_high_risk=false` 且不在 blocked）为 S 池晋级**必要条件**
  2. `blocked_count > 0` 的标的不得晋级
  3. 未审查标的不得晋级
- **文档**：补入 `docs/knowledge-base/01-系统架构/04-五池系统.md`
- **测试**：`tests/test_s_pool.py` 补准入测试（Skeptic通过/高风险/未审查/未blocked 四象限）
- **注意**：① 已在代码层实现必要条件1、2、3，⑥ 是把它升格为**明文契约 + 回归测试**，
  防止后续迭代把闸门回退掉。

---

## 复盘要点（供 S池准入链路评审）
1. **语义歧义**：S池注释写"决策已审查通过、S池只做记录"，但实际来源是重点观察池，
   而重点观察池里住着被 Skeptic 拦下的标的 —— "记事本"定位与"晋级"行为矛盾。
2. **双通道不对称**：决策层消费结构化裁决（JSON），S池写入层消费自由文本正则。
   同一标的在结构化通道被否决、在文本通道被放行，是脱节的结构性根源。
3. **兜底反噬**：07-21"有可执行交易信息即有效"的宽松兜底初衷是防格式漂移丢单，
   但 `any(k in window)` 纯子串匹配把"不操作"当成"操作"，把否决标的捞进 S 池。
   兜底逻辑必须有否定语境守卫，否则兜底会绕过上层所有闸门。
4. **自洽信号被丢弃**：决策报告尾部明写"S级操作池: 0只今日主推"，S池却写入3只。
   LLM 与池状态的一致性应作为最后闸门硬校验。
