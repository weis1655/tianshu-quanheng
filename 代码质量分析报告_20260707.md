# 天枢权衡系统 — 代码质量与遗留问题分析报告

> 分析日期：2026-07-07 | 分析对象：`agents/` 目录下 **43 个 Python 文件**（17,885 行）

---

## 一、硬编码阈值 / 魔法数字

### ✅ thresholds.py（单一真相来源，SSOT）
已集中管理所有阈值的文件。包含评分等级、过热检测、池容量、时间阈值等 30+ 常量。

### ❌ 阈值不一致 — 3 处确认问题

| # | 文件 | 行号 | 硬编码值 | 与 SSOT 差异 |
|---|------|:----:|:--------:|-------------|
| 1 | **pool_manager.py** | 1023 | `score >= 70` → **A级** | SSOT=`SCORE_A_LEVEL=75`。此处 70 导致 70-74 分标的被错误标为"A级"而非"黄色预警" |
| 2 | **pool_manager.py** | 669,760 | `max_tokens=600` | 无 SSOT 参考值，但 `base_agent.py` 默认 2000，差距大 |
| 3 | **decision_agent.py** | 238,240 | `decay_score = 70 - extra_penalty` / `max(55, 70 - extra_penalty)` | `70` 为硬编码，应引用 SSOT 常量 |

### ❌ 重复硬编码 — 已有 SSOT 仍用字面值

| 文件 | 行号 | 硬编码 | 对应 SSOT 常量 |
|------|:----:|:------|---------------|
| review_agent.py | 910-914 | 90, 75, 65, 55 | `SCORE_S_LEVEL`, `SCORE_A_LEVEL`, `SCORE_B_LEVEL`, `SCORE_C_LEVEL` |
| review_agent.py | 964-968 | 90, 75, 65, 55 | 同上（同一文件重复两次） |
| review_scorer.py | 35 | `WARN2_SCORE_FALLBACK = 70` | 应引用 `OVERHEAT_CRITICAL_SCORE` 或自建 SSOT |
| quality_gate.py | 24-28,58 | `"偏空": 85`, `"震荡偏弱": 80`, ... | 重复 `thresholds.DYNAMIC_SCORE_THRESHOLDS` |
| decision_agent.py | 374,521,614,1459 | `>= 75` | `DECISION_MIN_SCORE` |
| decision_agent.py | 1094,1459 | `>= 60` | `YELLOW_ALERT_MIN` |
| decision_agent.py | 605,714 | `max_tokens=1500` | 已知 P0 修复应改为 3000（第 58 项修复记录） |
| news_agent.py | 409 | `max_tokens=1800` | 无 SSOT |
| track_recorder.py | 65 | `>= 70` | `DECISION_MIN_SCORE=75`，不一致 |
| trigger_config.json | 5 | `"min_score": 70` | `DECISION_MIN_SCORE=75`，不一致 |

### 🔢 聚合统计：魔法数字使用频率

| 值 | 出现次数（agents/*.py） | 主要用途 |
|:--:|:----------------------:|---------|
| **70** | 15+ | 评分门槛、衰减基准、WARNING阈值 |
| **75** | 25+ | 升级门槛、准入阈值、DQP阈值 |
| **65** | 20+ | 降级门槛、B级下界 |
| **60** | 20+ | 硬性降级、黄色预警下界 |
| **80** | 10+ | PE阈值、偏空市场准入 |
| **85** | 6 | 追高拦截、偏空准入 |
| **50** | 10+ | 季涨门槛、基准分、重点池准入 |

---

## 二、异常处理审计

### 📊 try/except 使用统计

```
总 try 块：    165 个（分布在 27 个文件中）
命名 except：  82 个（捕获具体异常类型）
裸 except：     3 个（bare except，占 3.7%）
pass 语句：    57 个（在 15 个文件中）
```

### 🚨 裸 except 详情（严重性递减）

| 文件 | 行号 | 代码 | 评价 |
|------|:----:|------|------|
| **pool_updater.py** | 106-107 | `except:\n    pass` | 🔴 **最严重** — 静默吞掉所有异常，日期解析失败场合无日志 |
| **pool_manager.py** | 111-112 | `except:\n    return datetime.min` | 🟡 可接受（日期 fallback）但应限定 `ValueError` |
| **review_agent.py** | 1251-1252 | `except:\n    change_pct = 0.0` | 🟡 可接受但应限定 `ValueError` |

### 🚨 Swallowed Exception（`except Exception: pass` / `except: pass`）

- **decision_agent.py:1522-1523**: `except Exception:\n    records = []` — 静默吞掉文件读取异常
- **pool_updater.py:106-107**: `except:\n    pass` — 静默吞掉所有异常

### ✅ 良好实践
- `safe_file_utils.py` 的 4 个 except 均有明确类型和日志输出
- `market_agent.py` 的 7 个 except 均捕获 `Exception` 并打印错误

---

## 三、sys.path 硬编码与路径依赖

### 🔴 `sys.path.insert(0, ...)` — 14 次违规（应为 path_config 统一管理）

| # | 文件 | 行数 |
|---|------|:----:|
| 1 | news_agent.py | 29-30 |
| 2 | orchestrator.py | 23-24 |
| 3 | notifier.py | 16-17 |
| 4 | track_recorder.py | 21, 61, 141（3次） |
| 5 | pool_manager.py | 14 |
| 6 | skeptic_agent.py | 18-19 |
| 7 | screen_agent.py | 35-36 |
| 8 | decision_agent.py | 41-42 |
| 9 | trigger.py | 28 |
| 10 | review_agent.py | 34-35 |
| 11 | base_agent.py | 106-107 |
| 12 | research_agent.py | 163 |
| 13 | market_agent.py | 872 |
| 14 | statsmodels_analysis.py | 24 |

**共 17 个文件各自定义了 `PROJECT_ROOT`**（`Path(__file__).parent.parent.resolve()`），应统一到 `path_config.py`。

### 🔴 裸 `open()` — 绕过 `safe_file_utils`

| 文件 | 行号 | 代码 |
|------|:----:|------|
| **decision_agent.py** | 1328 | `with open(sm_file) as f:` |
| **decision_agent.py** | 1405 | `with open(sm_file) as f:` |
| **decision_agent.py** | 1518 | `with open(tracker_path) as f:` |
| **decision_agent.py** | 1544 | `with open(tracker_path, "w") as f:` |
| **statsmodels_analysis.py** | 356 | `Path(output_path).write_text(report)` |
| **trading_calendar.py** | 85 | `with open(CALENDAR_FILE, 'r') as f:` |
| **trading_calendar.py** | 253 | `with open(path, 'w') as f:` |

---

## 四、重复代码模式

### 🚩 完全相同的函数重复

1. **review_agent.py** — `_score_to_level()` 出现 **2 次**（L909-914 和 L963-968），代码 100% 相同
2. **review_agent.py** + **pool_manager.py** — 各有 `_score_to_level()`，但 **阈值不一致**（A级=75 vs 70）

### 🚩 阈值映射重复

- `quality_gate.py` 的 `MARKET_SCORE_THRESHOLDS` 与 `thresholds.py` 的 `DYNAMIC_SCORE_THRESHOLDS` 内容同步但独立维护
- `review_agent.py` 的 Prompt 模板（L91-152）中大量硬编码评分区间文本

### 🚩 三大巨无霸文件（27.8% 总代码量）

| 文件 | 行数 | 问题 |
|------|:----:|------|
| pool_manager.py | 1728 | 混合池管理+行情+LLM调用+评分逻辑 |
| decision_agent.py | 1716 | 混合决策+池联动+SkepticGate+模板兜底 |
| review_agent.py | 1530 | 混合审查+过热检测+池间流转+评分解析 |
| **合计** | **4974** | 缺乏模块拆分，单个文件责任过重 |

---

## 五、越权 / 权限校验缺失

| 问题 | 文件 | 详情 |
|------|------|------|
| **无 RBAC/权限模型** | 全系统 | 所有 Agent 均为等权限运行，无身份验证/授权校验 |
| **default=80 绕过 S 池准入** | decision_agent.py:233 | `original_score = r.get("综合分", 80)` — 当字段缺失时默认 80 分，绕过准入检查（已知 P0 修复为 continue 硬拒绝） |
| **trigger_config 阈值漂移** | trigger_config.json:5 | `"min_score": 70` 与 SSOT 的 `DECISION_MIN_SCORE=75` 不一致 |
| **config.yaml 配置冗余** | config.yaml | pools.max_watching=15 但 SSOT 池容量=20；screening 阈值与 SSOT 不匹配 |

---

## 六、CLAUDE.md 阈值参数保护区

**共 7 项受保护阈值**，修改前必须人工确认：

| # | 阈值 | 文件 | 值 |
|---|------|------|:--:|
| 1 | 审查升级 | review_agent.py | ≥75 |
| 2 | 决策准入 | decision_agent.py prompt | ≥75 |
| 3 | S级准入 | gate_controller.py / thresholds.S_POOL_MIN_SCORE | ≥75 |
| 4 | WARNING-1 过热 | review_agent.py L1050 | 日涨>8% + 综合分>75 |
| 5 | CRITICAL 过热 | review_scorer.py L91 | 月涨>25% + 综合分>=70 |
| 6 | 黄色预警区间 | decision_agent.py | 60-74 |
| 7 | 入池超14天淘汰 | screen_agent.py | 14天 |

**不在保护区但被大量硬编码的值**：`70`（A级降级/评分基线）、`65`（B级下界/自动降级）、`60`（硬性降级/黄色预警下界）

---

## 七、配置文件硬编码依赖

| 文件 | 硬编码项 | 风险 |
|------|---------|:----:|
| config.yaml | `worktree_base: "/tmp/auto_heal_worktrees"` | 依赖 /tmp 可写 |
| config.yaml | `file: "logs/tianshu.log"` | 相对路径依赖 cwd |
| config.yaml | `opencode_url: "https://opencode.ai/zen/v1/chat/completions"` | 外网 API endpoint 硬编码 |
| config.yaml | 阈值部分（circuit_breaker, win_rate, pnl_threshold） | 与 thresholds.py SSOT 未同步 |
| trigger_config.json | `stop_loss_pct: 5.0, take_profit_pct: 15.0, max_position_pct: 10.0, min_score: 70` | 与 SSOT 不一致，min_score=70≠75 |
| trading_calendar.py | `HOLIDAY_CALENDAR` 硬编码节假日字典 | 需每年手动更新 |

---

## 八、关键发现摘要

```
🔴 P0:
  - pool_manager.py A级阈值=70 而不是 75（SSOT 不一致）
  - decision_agent.py 裸 open() 4处绕过 safe_file_utils
  - pool_updater.py except: pass 静默吞所有异常
  - config.yaml + trigger_config.json 阈值与 SSOT 不同步

🟡 P1:
  - 3.7% bare except 比例（3/82）
  - 14 处 sys.path.insert(0) 未统一到 path_config
  - 17 处重复 PROJECT_ROOT 定义
  - review_agent.py 重复 _score_to_level() 函数
  - 57 个 pass 语句（15 个文件）可能导致异常被忽略
  - decision_agent.py max_tokens=1500 仍为原值（应 3000）

🟢 P2:
  - 三大巨无霸文件（pool_manager/decision_agent/review_agent >1500行）
  - config.yaml 与 SSOT 多字段不一致（池容量、筛选规则）
  - HOLIDAY_CALENDAR 硬编码需年度维护
  - 无 RBAC/权限模型（系统内应用场景）
```

---

## 九、数据来源

- 分析基础：`/home/seven/hermes-data/tianshu-quanheng/agents/` 下 43 个 Python 文件（17,885 行）
- SSOT 基准：`thresholds.py`（174 行，30+ 常量）
- 配置文件：`config.yaml`（159 行）、`data/trigger_config.json`（8 行）
- 保护区定义：`CLAUDE.md`（26 行）
- 搜索工具：ripgrep（`search_files`）、`grep`（`terminal`）
- 分析时间：2026-07-07