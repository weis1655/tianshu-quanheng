# 天枢权衡 — 数据全链路审计报告

> **审计日期**: 2026-07-24
> **审计范围**: 数据源 → 采集 → 转换 → 存储 → 校验，全链路覆盖
> **审计方法**: 代码静态分析 + 数据样本检查 + 故障案例库回溯

---

## 1. 数据源清单

### 1.1 行情数据源

| # | 源名称 | 类型 | 数据格式 | 关键字段 | 获取方式 | 更新频率 | 依赖模块 |
|---|--------|------|----------|----------|----------|----------|----------|
| 1 | 腾讯行情API (`qt.gtimg.cn`) | 实时快照 | 文本(GBK, `~`分隔) | code/name/price/chg_pct/high/low/volume/pe/amplitude/vol_ratio/month_chg/quarter_chg | `QuoteProvider.fetch_quote()` → HTTP GET → `~` split → `safe_float` | 实时(每次调用) | `quote_provider.py` |
| 2 | 东方财富批量行情 (`push2.eastmoney.com`) | 批量行情 | JSON | f12/code, f14/name, f3/price, f25/chg_pct, f20/market_cap, f9/pe, f38/turnover, f41/rev_yoy, f46/np_yoy + 10+字段 | `QuoteProvider.fetch_all_stocks()` → HTTP GET → `json.loads` | 实时(每次调用, 分页60页) | `quote_provider.py` |
| 3 | 新浪K线API (`money.finance.sina.com.cn`) | 历史K线 | JSON | day/open/high/low/close/volume | `QuoteProvider.fetch_kline()` → HTTP GET → `json.loads` | 历史数据(调参) | `quote_provider.py` |
| 4 | 腾讯日K线 (`web.ifzq.gtimg.cn`) | 历史K线(含今日) | JSONP(需剥离前缀) | 日期/开盘/最高/最低/收盘/成交量 | `fetch_tencent_history()` → HTTP GET → `re.sub`剥离→ `json.loads` | 历史数据(调参) | `market_agent.py` |
| 5 | 东方财富历史K线 (`push2his.eastmoney.com`) | 历史K线(含今日) | JSON(klines为CSV行) | 日期/开盘/最高/最低/收盘/成交量 | `fetch_em_history()` → HTTP GET → `line.split(',')` | 历史数据(调参) | `market_agent.py` |
| 6 | 东方财富涨幅榜 (`push2.eastmoney.com`) | 实时涨幅榜 | JSON | f2/f3/f5/f6/f8/f12/f14/f15/f20 | `ScreenAgent._scan_technical_signals()` → HTTP GET | 实时(每次调用) | `screen_agent.py` |
| 7 | 新浪涨幅榜 (`vip.stock.finance.sina.com.cn`) | 实时涨幅榜(降级) | JSON(GBK) | code/changepercent/turnover/volume_ratio/amplitude | `ScreenAgent._scan_technical_signals()` fallback → HTTP GET | 降级备用 | `screen_agent.py` |

### 1.2 新闻数据源

| # | 源名称 | 类型 | 数据格式 | 关键字段 | 获取方式 | 更新频率 | 依赖模块 |
|---|--------|------|----------|----------|----------|----------|----------|
| 8 | DuckBurn新闻联播 (GitHub) | 联播完整版 | Markdown | 全文(含新闻联播关键词) | `fetch_from_duckburn()` → `requests.get` → 质量检查 | 日更(今天/昨天/前天兜底) | `news_agent.py` |
| 9 | govopendata.cn | 联播HTML备用 | HTML(需剥离标签) | 全文(联播/新闻关键词) | `fetch_from_govopendata()` → `requests.get` → `re.sub`去HTML标签 | 日更(今天/昨天) | `news_agent.py` |
| 10 | 新浪快讯 (`zhibo.sina.com.cn`) | 实时快讯(白天首选) | JSON | create_time/rich_text | `fetch_from_sina_news()` → `requests.get` → 筛选今日 | 实时(白天) | `news_agent.py` |
| 11 | 同花顺快讯 (`news.10jqka.com.cn`) | 实时快讯(白天备用) | JSON | title/time | `fetch_from_tonghuashun()` → `requests.get` → 今日筛选 | 实时(白天) | `news_agent.py` |
| 12 | 东方财富快讯 (`np-listapi.eastmoney.com`) | 全天候快讯(最稳定) | JSON (通用) | title/content/text | `fetch_from_eastmoney()` → `requests.get` → 通用JSON提取 | 全天候 | `news_agent.py` |

### 1.3 LLM API

| # | 源名称 | 类型 | 数据格式 | 获取方式 | 频率 | 依赖模块 |
|---|--------|------|----------|----------|------|----------|
| 13 | 主LLM (SenseNova/自定义) | 文本生成 | 纯文本/结构化 | `BaseAgent.call_llm()` → 指数退避重试(3次) → fallback flash-lite | 4次/日(News+Screen+Review+Decision) + Skeptic | `base_agent.py` |
| 14 | ML评分模型 | 评分预测 | 数值 | `ml_scorer.py` → 因子数据 → 评分 | 每日1次 | `ml_scorer.py` |

### 1.4 其他数据源

| # | 源名称 | 类型 | 数据格式 | 获取方式 | 频率 | 依赖模块 |
|---|--------|------|----------|----------|------|----------|
| 15 | 硬规则库 (`规则库/硬规则.md`) | 静态规则 | Markdown | 文件读取 → 关键词匹配 | 按需加载 | `orchestrator.py` |
| 16 | 配置文件 (`config.yaml`) | 配置 | YAML | `yaml.safe_load()` | 启动时 | `pool_manager.py` |

---

## 2. 数据存储节点

### 2.1 五池JSON（核心数据存储）

| 文件 | 路径 | 格式 | 核心字段 | 更新时机 | 容量上限 |
|------|------|------|----------|----------|----------|
| **快筛候选池** | `五池管理/快筛候选池.json` | JSON | `stocks[]`(代码/名称/纳入日期/驱动来源/最新价/涨跌幅/换手率/量比/综合分), `_fast_screen_history{}`, `统计`, `历史记录[]` | 每日快筛后(screen_agent.py) | 20只 |
| **重点观察池** | `五池管理/重点观察池.json` | JSON | `stocks[]`(代码/名称/综合分/信心度/驱动级别/核心逻辑/纳入日期/今日收盘/今日涨跌/换手率/量比/振幅/更新时间), `统计`, `历史记录[]` | 审查后升级(review_agent.py), 每30分钟价格刷新(pool_price_refresh.py) | 20只 |
| **S级操作池** | `五池管理/S级操作池.json` | JSON | `stocks[]`(代码/名称/综合评分/纳入日期/驱动来源/核心逻辑/入场价/T+1验证等), `统计`, `历史记录[]` | 决策后(pool_updater.py) | 3只 |
| **持仓池** | `五池管理/持仓池.json` | JSON | `stocks[]`(空), `历史持仓[]`(代码/名称/建仓价/持仓数/盈亏/止损原因), `资金配置`, `统计`, `入池记录[]` | 盟主手动更新 | 无上限 |
| **边缘池** | `五池管理/边缘池.json` | JSON | `stocks[]`(代码/名称/综合分/降级时间), `统计`, `历史记录[]` | 降级触发(sweep_downgrade.py/review_agent.py) | 30只 |
| **重点观察池_历史池** | `五池管理/重点观察池_历史池.json` | JSON | `stocks[]`(移出记录), `历史记录[]` | 移出重点池时追加 | 200只 |

### 2.2 数据文件

| 文件 | 路径 | 格式 | 内容 | 更新时机 |
|------|------|------|------|----------|
| **决策日志** | `data/decision_log.json` | JSON(数组) | code/name/entry_price/actual_pnl/drive_type/date/recommendation/confidence/tech_score/fundamental_score/hypothesis/verify_date/actual_change/reflection | 每次决策后 |
| **熔断器状态** | `data/circuit_breaker_state.json` | JSON | state/consecutive_failures | 每轮检查后 |
| **共享内存** | `data/shared_memory.json` | JSON | 跨Agent共享上下文 | 每轮执行 |
| **闭环追踪** | `data/闭环追踪/{date}_闭环追踪.json` | JSON | 快筛→审查→决策全流程追踪 | 每个Agent执行后 |
| **回测任务** | `data/backtest_workbench/tasks/tasks.json` | JSON | 回测任务列表 | 按需创建 |
| **回测结果** | `data/backtest_workbench/results/BT*.json` | JSON | 回测结果数据 | 回测完成后 |

### 2.3 报告文件（Markdown）

| 文件 | 路径 | 格式 | 内容 | 更新时机 |
|------|------|------|------|----------|
| **宏观前置分析** | `data/历史记录/{date}_宏观前置分析.md` | Markdown | 新闻摘要 + 驱动分级 + 传导链分析 | 新闻Agent每日06:20 |
| **快筛报告** | `data/历史记录/{date}_快筛报告.md` | Markdown | 宏观摘要 + 快筛分层结果 + 候选池更新 | 快筛Agent每日07:10 |
| **审查报告** | `data/历史记录/{date}_审查报告.md` | Markdown | 候选池审查结果 + 四维评分 + 池更新 | 审查Agent每日07:10 |
| **决策报告** | `data/历史记录/{date}_决策报告.md` | Markdown | 执行方案(主推/备选) + 仓位/止损/止盈 | 决策Agent每日07:10 |
| **质疑审查报告** | `data/历史记录/{date}_质疑审查报告.md` | Markdown | 五维质疑分析 | SkepticAgent |
| **质疑审查裁决** | `data/历史记录/{date}_质疑审查裁决.json` | JSON | blocked[] + gate_passed | SkepticAgent |
| **反馈闭环报告** | `data/历史记录/{date}_反馈闭环报告.md` | Markdown | 持仓分析 + 多窗口胜率对比 | FeedbackLoop |

### 2.4 其他存储

| 文件 | 路径 | 格式 | 更新时机 |
|------|------|------|----------|
| 备份(五池) | `五池管理/__backups__/*.bak.*` | JSON | 每次save_pool时(保留3份轮转) |
| 备份(池) | `data/backups/pools/{timestamp}/` | JSON | 每日定时 |
| 备份(报告) | `data/backups/reports/{timestamp}/` | JSON/MD | 每日定时 |
| 日志 | `logs/*.log` | 文本 | 每次运行时 |
| 交易导出 | `trades_export.csv` | CSV | 按需导出 |

---

## 3. 数据转换节点

### 3.1 行情数据转换

| 位置 | 转换逻辑 | 输入 | 输出 |
|------|----------|------|------|
| `quote_provider.py:fetch_quote()` | 腾讯API `~`分隔符文本解析 → `split('~')` → `safe_float/safe_int` | GBK文本(含40+字段) | 结构化dict(12字段) |
| `quote_provider.py:parse_stock_item()` | 东方财富JSON字段映射(f12→code, f14→name, ...) + `safe_float` | 东方财富f-字段dict | 标准化dict(14字段) |
| `market_agent.py:fetch_history()` | 新浪K线JSON → `round(float(x),2)` | JSON数组 | 清洗后K线列表 |
| `market_agent.py:fetch_tencent_history()` | JSONP剥离(`re.sub`) → `json.loads` → tuple索引解析 | JSONP文本 | 清洗后K线列表 |
| `market_agent.py:fetch_em_history()` | CSV行(`line.split(',')`) → `round(float)` | 东方财富klines数组 | 清洗后K线列表 |
| `screen_agent.py:_scan_technical_signals()` | 涨幅数据归一化(`f3/100`判断) → 多条件过滤(`>5% & >8%换手 & >1.5量比 & >5%振幅 & >50亿市值`) | 东方财富涨幅榜JSON | 候选股列表 |
| `screen_agent.py:_update_candidate_pool()` | 正则提取股票代码(`名称(代码)`6种格式) → 代码验证 → 去重 → 48h防护 → 跨池防护 → 过期淘汰 | LLM输出文本 | 候选池stocks[] |

### 3.2 新闻数据转换

| 位置 | 转换逻辑 | 输入 | 输出 |
|------|----------|------|------|
| `news_agent.py:fetch_from_govopendata()` | HTML标签剥离(`re.sub('<[^>]+>', '')`) → JS/CSS噪音过滤 → 截断5000字 | HTML | 纯文本摘要 |
| `news_agent.py:fetch_from_sina_news()` | JSON嵌套解析(`data.result.data.feed.list`) → 今日筛选 → 格式化 | 新浪JSON | 格式化快讯文本 |
| `news_agent.py:fetch_from_tonghuashun()` | 多级JSON结构处理 → 今日筛选 → 格式化 | 同花顺JSON | 格式化快讯文本 |
| `news_agent.py:fetch_from_eastmoney()` | 通用JSON提取(递归遍历values) → item提取(title/content/text) → 非JSON则HTML去标签 | 东方财富API响应 | 格式化快讯文本 |
| `news_agent.py:fetch_news_broadcast()` | 多源智能合并(按时段优先) → 联合排序 → 取前2源合并 | 多源文本 | 合并后新闻 |
| `news_agent.py:is_valid_news()` | 字数检查(300/500) → 失败标记检测(404/Not Found/无新闻数据) → 格式检测(联播/快讯) → 条数检查 | 原始新闻文本 | (bool, 诊断信息) |
| `news_agent.py:_parse_drivers()` | 3种格式兼容解析(旧格式/新格式/混合) → 正则提取S/A/B/C级 → 截断10条 | LLM输出 | 结构化驱动分级 |
| `news_agent.py:_parse_chains()` | 箭头归一化(`→`统一) → 传导链关键词提取 → 3级降级(传导链→箭头→驱动分级→兜底) | LLM输出 | 传导链分析文本 |

### 3.3 评分/计算转换

| 位置 | 转换逻辑 | 输入 | 输出 |
|------|----------|------|------|
| `review_scorer.py:OverheatDetector.detect()` | 7条过热规则链(CRITICAL×3 + WARNING×4) → 强市豁免(WARNING跳过) → 扣分计算 | 涨幅/PE/换手/量比/月涨/季涨/评分/振幅/市场状态 | (overheat_level, penalty, reason) 或 None |
| `review_agent.py:_parse_review_result_v2()` | V2正则提取(综合评分/信心度/流转方向) → ML评分注入 → 因子信号加分 → 市场状态通缩 → QualityGate质检 → 评分调整 | LLM输出+因子数据 | 结构化ReviewResult |
| `review_agent.py:_apply_pool_updates()` | 根据upgrades/demotions执行池间流转 → 过期淘汰 → 重复防护 | ReviewResult | 池文件更新 |
| `decision_agent.py:_inject_evo_history()` | 历史决策摘要注入(记忆闭环) → 评分过滤(≥75) → 黄色预警(60-74) | 审查报告+历史记录 | LLM上下文 |
| `pool_updater.py:update_s_pool()` | 正则提取【主推】标的 → 推荐买入价提取 → 价格位置检查 → 代码去重合并 → 容量限制3只 | 决策报告文本 | S池写入 |
| `quality_gate.py:check()` | 市场状态动态阈值 → 历史表现回溯扣分 → 冷却期检查 → 过热二次检测 → 连续推荐限制 | 评分+市场状态+历史 | (passed, reason, adjusted_score) |
| `pool_manager.py:save_pool()` | 容量限制检查 → 自动排序(综合分降序) → 持仓数同步 → 备份轮转(保留3份) | 池数据dict | 写入文件+备份 |
| `pool_manager.py:refresh_key_watch_prices()` | 读取池中股票 → 批量拉行情 → 更新价格字段 → 评分衰减(每7天-0.5分/天, 下限40) → 降级扫描 | 池JSON+行情API | 更新后池JSON |
| `closed_loop_tracker.py` | 快筛→审查→决策三阶段追踪 → 流转状态记录 → 闭环追踪JSON | 各Agent输出 | 追踪JSON |

### 3.4 正则提取（关键数据转换点）

| 位置 | 正则表达式 | 用途 | 风险等级 |
|------|-----------|------|----------|
| `screen_agent.py:L332` | `([\u4e00-\u9fa5]{2,6})\s*[（(](\d{6})[）)]\s*[-–—]\s*([^\n]{1,80})` | 提取候选股票(名称+代码+理由) | **高**-格式漂移频繁 |
| `screen_agent.py:L334` | `(\d{6})\s+([\u4e00-\u9fa5]{2,6})\s*[-–—]\s*([^\n]{1,80})` | 兜底格式(代码在前) | 高 |
| `pool_updater.py:L35` | `【主推】\s*([\u4e00-\u9fa5]{2,6})\s*[（(](\d{6})[）)]` | 提取S池主推标的 | **高**-依赖LLM输出格式 |
| `pool_updater.py:L81` | `{name}\s*[（(]{code}[）)][^$]*?买入价[：:]\s*([\d.]+)` | 提取推荐买入价 | 高 |
| `news_agent.py:L485` | `-\s+\*\*([^*]+)\*\*\s*[：:]\s*(.+?)(?=\n-\s+\*\*|\n##|\n$)` | 提取驱动分级 | 高 |
| `review_agent.py` | 多级V2正则(综合评分+信心度+流转方向) | 提取审查评分 | **高**-P0格式漂移根因 |

---

## 4. 历史数据质量问题（63+故障案例）

### 4.1 P0-格式漂移（最高频，占P0的30%+）

| 日期 | 问题 | 根因 | 影响 |
|------|------|------|------|
| 07-24 | 23个P0-降级延迟假阳性 | 审查报告格式漂移，`extract_review_results()`正则未匹配 | 回头看报告误报 |
| 07-17 | 3只候选池标的V2评分提取失败→score=0 | LLM输出格式系统性漂移，正则未覆盖 | 错误降级 |
| 07-15 | V2正则失败 | LLM输出格式变化 | score=0级联异常 |

### 4.2 P0-降级延迟（占P0的25%+）

| 日期 | 问题 | 根因 | 影响 |
|------|------|------|------|
| 07-07 | 89个P0降级延迟 | 评分调整后无降级检查 | 低分标的滞留 |
| 07-09 | 中科曙光8天从73衰减至63未降级 | 衰减公式错误(基于衰减后分数再次衰减) | 评分失真 |
| 06-18 | 14只评分<60标的滞留候选池 | 评分调整后低分不触发降级 | 候选池污染 |

### 4.3 P0-决策异常

| 日期 | 问题 | 根因 | 影响 |
|------|------|------|------|
| 05-18 | 审查降级→决策依然买入 | fallback逻辑覆盖审查结果 | 越权买入 |
| 06-10 | LLM说空仓后模板兜底强制买入，8/8全亏 | 偏空市场无保护 | 100%亏损 |
| 07-21 | 连续多天空仓报告被替换 | 退化判定过严 | 误报退化 |
| 07-21 | S池入场价用实时行情而非推荐价 | 正则提取失败 | 入场价偏差 |

### 4.4 P0-LLM退化

| 日期 | 问题 | 根因 | 修复 |
|------|------|------|------|
| 07-06 | 决策报告零方案，252行空报告 | max_tokens=1500不足 | 改为3000 |
| 07-20 | 退化报告复发 | 模型自身输出格式偏差 | `_validate_decision_output()`校验 |
| 06-25 | 报告写到第4只被截断 | max_tokens=1500 | 改为3000 |

### 4.5 P0-池异常

| 日期 | 问题 | 根因 |
|------|------|------|
| 06-04 | S池3只全部与重点池重叠 | 三层防线全缺失 |
| 06-30 | S级操作池18天空池 | 三道防线重复拦截 |
| 07-21 | 中科曙光入S池后被空仓报告替换但未移除 | 记事本模式无回滚 |
| 06-30 | ML评分<45的标的被LLM高分覆盖升级 | LLM评分主导 |

### 4.6 P0-过热漏检

| 日期 | 问题 | 根因 |
|------|------|------|
| 06-16 | 长川科技月涨+15.3%评分70，入池后3日跌-6.46% | WARNING规则基于单日涨幅，累积涨幅被绕过 |
| 07-21 | 今日刚入S池的标的被降级 | sweep_downgrade无今日保护 |

### 4.7 数据质量关键发现

- **边缘池重复数据严重**: 历史记录中同一个标的被多次移除（如中际旭创300308在07-19被移除3次、浪潮信息000977被移除2次、亿华通688339被移除4次等），说明去重机制失效
- **综合分0.0大量出现**: 边缘池移除记录中大量标的综合分为0.0，说明评分数据未正确传递到边缘池
- **快筛候选池历史记录膨胀**: 累计700+条记录，但多数标的从未进入后续池
- **决策日志 zero pnl 比率高**: 监控脚本检测到80%+无盈亏数据

---

## 5. 当前数据校验机制

### 5.1 安全类型转换（`safe_file_utils.py`, `quote_provider.py`）

```python
# 安全转换函数（4处）
safe_float(v, default=0.0)    # 处理 None/'-'/''/ValueError
safe_int(v, default=0)        # 同上
safe_str(v, default="")       # 处理 None → strip
safe_read_json(file, default=None)     # IOError/JSONDecodeError 兜底
safe_read_text(file, default="")       # FileNotFoundError 兜底
safe_write_file(file, content)         # IOError 捕获
safe_append_file(file, content)        # IOError 捕获
```

### 5.2 新闻质量门控（`news_agent.py:is_valid_news()`）

- 字数下限(快讯300/联播500)
- 有效新闻条数≥5(快讯格式)或联播特征检测
- 失败标记检测(404/Not Found/无新闻数据/请求失败/DOMContentLoaded/querySelector)
- 多源降级链(5个源 → 3个时段策略 → 兜底遍历)

### 5.3 指数退避重试（`base_agent.py:call_llm()`）

- LLM调用失败自动重试3次
- 主模型失败 → 降级至flash-lite
- 连续退化计数器 → `force_rule_mode`

### 5.4 数据质量监控脚本（`scripts/data_quality_monitor.py`）

- 池空异常检测
- 重复代码检测
- 统计更新日期检查(>3天未更新告警)
- JSON解析异常检测
- 决策日志盈亏数据完整性检查(>80% zero pnl告警)
- 熔断器状态检查

### 5.5 池操作校验（`pool_manager.py`）

- 容量限制检查(自动截断/清理过期)
- 自动排序(综合分降序)
- 持仓数同步(统计与stocks一致)
- 备份轮转(保留3份)
- 日期格式异常跳过(ValueError保护)
- 脏数据降级(非标准字段兼容)

### 5.6 评分校验链（`review_agent.py`）

- 因子信号加分(≥3信号触发, 上限2分)
- 市场状态通缩(偏空-8, 震荡偏弱-5, 震荡-3)
- 弱市因子减半
- QualityGate历史表现检查(亏损扣分, 冷却期)
- ML评分低信心标记(ML<45且LLM≥75 → 背离标记)
- ML评分降级(LLM高分但ML低分 → 改为降级)
- 评分=0强制降级(score=0且flow_dir≠降级时强制)
- 打分膨胀控制(总上限100分)

### 5.7 过热检测（`review_scorer.py:OverheatDetector`）

- 7条规则链(CRITICAL×3 + WARNING×4)
- 强市豁免(偏多/震荡偏强跳过低级WARNING)
- 边界修复(>= 而非 > 防止70分漏检)
- 高波动补漏(RULE-7: 振幅>7%+月涨>15%+评分>=70)

### 5.8 去重机制

| 防护层 | 模块 | 机制 |
|--------|------|------|
| 48小时重复筛选 | `screen_agent.py` | `_fast_screen_history` 记录, 同代码48h内跳过 |
| 跨池重复防护 | `screen_agent.py` | PoolManager遍历所有池, 已存在则跳过 |
| 候选池过期淘汰 | `screen_agent.py` | 入池>14天未升级自动移除 |
| 边缘池过期淘汰 | `pool_manager.py` | 入池>30天自动移除 |
| 代码去重合并 | `pool_updater.py` | S池写入时按code去重 |
| 三振出局 | `gate_controller.py` | 连续阻塞≥3次→自动移入边缘池 |
| 首次high豁免 | `gate_controller.py` | 首次阻塞(blocked_count=0)且非veto→只标记不阻断 |

### 5.9 空值/异常兜底

- 行情获取失败 → 返回空列表/None（不阻塞流程）
- 池文件JSON解析失败 → 返回空池结构
- 数据源fail → 自动降级到下一个源(新闻5源降级链)
- 日期格式异常 → `try/except ValueError`跳过该标的
- 代码验证失败 → 宽松模式保留所有(6位数字已足够可靠)
- 退市/ST/亏损检查 → 关键词匹配(`orchestrator.py:check_hard_rules()`)
- 熔断器 → `circuit_breaker_state.json` 记录连续失败次数

---

## 6. 审计发现总结

### 6.1 高风险项

1. **LLM输出格式漂移是最大风险源**：占P0故障的30%+，正则提取的高脆弱性是系统阿喀琉斯之踵
2. **边缘池数据质量差**：大量重复记录、综合分=0.0的标的，去重和评分传递机制有缺陷
3. **决策日志数据完整性不足**：80%+记录无盈亏数据，无法有效支撑回测和复盘
4. **无架构级数据校验层**：校验逻辑散落在各模块中，没有统一的数据质量框架

### 6.2 中风险项

5. **五池字段命名不统一**：`代码`/`股票代码`、`名称`/`股票名称`、`综合分`/`综合评分`/`技术面评分`混用
6. **行情API缓存失效**：`QuoteProvider`缓存TTL=300秒，多个Agent独立调用导致重复请求
7. **无数据血缘追踪**：无法追溯某个标的的完整流转历史
8. **ML评分与LLM评分不一致**：ML<45且LLM≥75的背离场景已发现但未完全解决

### 6.3 低风险项

9. 备份轮转机制完善(3份+每日定时备份)
10. 安全文件操作已统一封装(`safe_file_utils`)
11. 阈值已集中管理(`thresholds.py` SSOT)
12. 硬规则防护已下沉到各Agent